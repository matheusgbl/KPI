import styled from 'styled-components';

export const Container = styled.section`
  padding: 2rem 20%;
  display: flex;
  flex-direction: column;

  .write_question {
    resize: none;
    width: 100%;
    outline: none;
  }

  .finalize-questions-btn {
    margin-top: 1rem;
    border-radius: 4px;
  }
`;
